"""API Gateway package.

Hosts the FastAPI application factory (`main.py`), configuration loading
(`config.py`), and structured logging setup (`logging.py`). This is the
single entry point every other backend component attaches to, starting with
this phase (Phase 4 — Backend Foundation).
"""
